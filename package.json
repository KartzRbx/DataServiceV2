{
  "private": true,
  "workspaces": ["www"],
  "scripts": {
    "docs": "npm run dev -w keepdata-docs",
    "docs:build": "npm run build -w keepdata-docs",
    "keepdata:generate": "node scripts/keepdata-generate.mjs",
    "keepdata:check": "node scripts/keepdata-generate.mjs",
    "dataservice:generate": "npm run keepdata:generate",
    "dataservice:check": "npm run keepdata:check"
  }
}
